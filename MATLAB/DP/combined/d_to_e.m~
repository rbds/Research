function [ dist, loc ] = d_to_e( pos, sources )
%d_to_e find distance to nearest energy source.
%   Detailed explanation goes here

% dist = norm(repmat(robot.p(1:2)', size(sources,1), 1),  - sources);
dist = zeros(length(sources),1);



end

